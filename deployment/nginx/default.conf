server { listen 80; client_max_body_size 1m; location /static/ { proxy_pass http://frontend/; } location / { proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; proxy_pass http://backend:8080; } }
